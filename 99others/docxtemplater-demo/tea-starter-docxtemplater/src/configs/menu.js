/**
 * 定义导航菜单
 * @type {import('@tea/app/types').AppMenu}
 */
export const menu = {
  title: "DOCXTEMPLATER",
  items: [
    { route: "/", title: "Home" },
    { route: "/users", title: "Users" }
  ]
};
